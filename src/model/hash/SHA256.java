package model.hash;

public class SHA256 extends AFileHash {

    public SHA256() {
        super("SHA-256", "");
    }

    public SHA256(String provider) {
        super("SHA-256", provider);
    }
}
